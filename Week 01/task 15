//task15
int main()
{
int str1, str2, str3;
	std::cin >> str1 >> str2 >> str3;
	bool isTriangle = ((str1 + str2 > str3) && (str2 + str3 > str1) && (str1 + str3 > str2));
	std:: cout << (isTriangle ? "is triangle" : "isn't triangle");
}
